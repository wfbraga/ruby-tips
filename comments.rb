# Tip: Use `=begin` / `=end` for block comments (they must start in column 1 and are mainly for RDoc).
# Alternatively prefix each line with `#`, or wrap in `if false ... end` to temporarily disable a block.
# A heredoc assigned to a variable (or `nil`) can also serve as a block comment when needed.
# The code below adds 2 numbers together
# Another comment right here
# Another comment down below
puts 1 + 1
puts 2 + 2 # This will still work
puts 3 + 3